function changeColor(c) {
    // Get the element with the text that we want to change
    let colorText = document.getElementById("color-text")

    // Set the color to the incoming value
    colorText.style.color = c
}
